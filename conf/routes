# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


GET  	      /                           @controllers.AllocationController.listAllocations
# GET         /                           @controllers.AllocationController.listAllocations
# POST        /allocations/:id/notify     @controllers.AllocationController.sendReminderOfAllocationWithID(id: String)
#
# GET         /persons                       @controllers.CrewController.showCrew
# POST        /persons/add                   @controllers.CrewController.addPerson
# POST        /persons/:id                   @controllers.CrewController.updatePerson(id: Int)
# POST        /persons/:id/delete            @controllers.CrewController.deletePerson(id: Int)
#
# POST        /taskoverview/add                  @controllers.TaskController.addAndAllocateTask
# POST        /taskoverview/:id                  @controllers.TaskController.updateTask(id: String)
# POST        /taskoverview/:id/delete           @controllers.TaskController.deleteTask(id: String)
#
# GET         /dropbox                    @controllers.Dropbox.index
# GET         /dropbox/test               @controllers.Dropbox.uploadFileTest
# GET         /dropbox/auth-finish        @controllers.Dropbox.authFinish(code: String ?="", state: String ?="")


# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file               controllers.Assets.at(path="/public", file)

